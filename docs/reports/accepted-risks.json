{
  "schema": "roamlink/accepted-risks@1",
  "note": "Explicit, visible accepted-risk records for the RoamLink production readiness gate (RL-081). Recorded by Worker C at gate-construction time; ratification by the Tech Lead is required at merge. These records NEVER weaken a gate criterion: every criterion is still evaluated literally — an unmet row covered here is reported as a `gap` row carrying its risk ids, and any uncovered failure still fails the gate. The MVP gate (RL-080) has NO acceptance mechanism by dispatch: its failing criteria are findings for the Tech Lead, and the headline MVP finding (the §11 SLO criterion, see AR-002) is what currently fails RL-080.",
  "risks": [
    {
      "id": "AR-001",
      "title": "Dogfood scenarios do not wire the observability structured logger (correlation-ID logging verified via audit events only)",
      "criterion": "PRD-3",
      "rowId": "OBS:logs-correlation",
      "severity": "medium",
      "justification": "spec/architecture.md and definition-of-done 'Operations' require structured logs with correlation IDs where applicable. The @roamlink/observability logging contract (redacting structured records, correlated logger over an injectable carrier) is implemented and unit-verified (packages/observability tests: logger, log-redaction), and the RL-072 dogfood scenarios DO prove one correlation-ID family end-to-end — but through AUDIT events and command envelopes, not through emitted structured-log records: the dogfood world (tests/dogfood/src/world.ts) never imports @roamlink/observability. Closing this requires editing tests/dogfood (RL-072's completed work-item package), which is outside the release-gate worker's dispatched scope (scripts/release, tests/release-gates, docs/reports, README/CHANGELOG only).",
      "exposure": "No runtime risk. The gap is verification coverage: a future regression that drops correlation IDs from log records would not be caught by the dogfood suite. Correlation-ID propagation itself IS proven end-to-end through the audit chain.",
      "remediation": "Wire createCorrelatedLogger + createInMemoryLogSink into makeDogfoodWorld, thread the scenario's correlation family through the carrier, and assert emitted records carry the correlation id (one focused addition to tests/dogfood; a small follow-up work item for the Tech Lead).",
      "owner": "Tech Lead",
      "reviewBy": "first post-RL-081 wave",
      "references": ["GAP:dogfood-structured-logging"]
    },
    {
      "id": "AR-002",
      "title": "No §11 SLO is instrumented or referenced in the dogfood/load suites (the current MVP-gate blocker)",
      "criterion": "PRD-3",
      "rowId": "OBS:slo-wiring",
      "severity": "high",
      "justification": "spec/architecture.md §11 lists nine SLOs the product must measure. The observability primitives (RL-052: makeServiceLevelObjective, SloEventRecorder, burn rates, health/metrics/log composition) can instrument every one of them — proven green by tests/release-gates/test/contract-deep.test.ts, which constructs and evaluates all nine through the real primitives — but NO dogfood or load assertion references any §11 SLO name, and no product surface wires §11-named SLOs (only the app-kit UI fake seeds time_to_usable_connectivity). This is exactly why the MVP release gate (RL-080) currently FAILS its SLO criterion: 'every SLO in spec/architecture.md §11 instrumented by observability primitives with at least one dogfood/load assertion referencing it.' Closing it requires editing tests/dogfood and/or tests/load (RL-072/RL-073's completed work-item packages), outside the release-gate worker's dispatched scope. Recorded here so the production gate's evidence is complete and the blocker is visible, not hidden; the MVP gate itself remains failing until the wiring lands.",
      "exposure": "Verification-coverage gap, not a runtime defect: the release ships without mechanical SLO evidence from the end-to-end suites. The strongest available proxy IS verified: the observability SLO machinery composes correctly (health/metrics/logging, no-data never healthy) and the deployment suite proves SLO health composition (H-3/H-4).",
      "remediation": "In tests/dogfood (and/or tests/load): construct §11-named SLOs through @roamlink/observability, record good/bad outcomes at the scenario's deterministic clock instants (e.g. time-to-usable-connectivity measured across the onboarding journey; failover-success across scenario 2's recovery loop; stale-duration across the delay windows), and assert the evaluations. A small follow-up work item for the Tech Lead; once landed, the MVP gate's MVP-3 criterion turns green without any gate change (the gate scans for exactly these references).",
      "owner": "Tech Lead",
      "reviewBy": "immediately (blocks RL-080 pass)",
      "references": ["GAP:slo-instrumentation-dogfood-load", "spec/architecture.md#11-slos"]
    },
    {
      "id": "AR-003",
      "title": "spec/current-state.md still claims 'no production feature implementation started' / 'Repository state: greenfield'",
      "criterion": "PRD-4",
      "rowId": "DOCS:current-state",
      "severity": "medium",
      "justification": "The implemented tree contradicts spec/current-state.md's header claims: 25 packages + 3 apps + 7 verification suites with green tests exist on main. The dispatch for the release-gate worker explicitly forbids editing spec/* ('the gates MEASURE the spec, they don't change it'), so this documented inconsistency is recorded as an explicit gap for the Tech Lead instead of being silently patched.",
      "exposure": "Documentation only: a reader of spec/current-state.md is misled about implementation status. No code, test or gate behavior depends on the stale claims (the gates verify the tree, not the doc's prose).",
      "remediation": "The Tech Lead refreshes spec/current-state.md's status block to reflect the implemented tree (minutes of work); the production gate's DOCS:current-state row then turns green without any gate change.",
      "owner": "Tech Lead",
      "reviewBy": "at merge of the release-gates branch",
      "references": ["GAP:spec-current-state-stale"]
    },
    {
      "id": "AR-004",
      "title": "RL-074-F1: secret-shaped provider references are accepted into commerce payment records",
      "criterion": "PRD-1",
      "rowId": "SEC:findings-recorded",
      "severity": "medium",
      "justification": "A malicious provider returning a JWT-shaped string as its payment 'provider reference' is accepted at admission and persisted into the commerce read model (charset permits dots/base64url). spec/data-model.md 'Privacy' says secrets/credentials are never persisted in ordinary domain tables. Exposure is bounded: the value is provider-supplied opaque REFERENCE data, not a RoamLink credential; PEM-shaped values are rejected by the charset; the repo's own RL-054 scanner flags the persisted record afterward (detection exists, admission-time enforcement does not). Fixing it means editing packages/domain-commerce — explicitly outside the release-gate worker's scope — so the finding is accepted for this release with the recorded remediation.",
      "exposure": "A secret-shaped value could be persisted in a commerce record and surfaced in read models; it is not a usable credential and is detectable by the existing scanner sweep.",
      "remediation": "Run assertNoSecretMaterial over provider-supplied reference fields at commerce admission, or further restrict the charset (e.g. no '.'). Minimal reproducer already pinned in tests/security S-6 so the fix flips an explicit assertion.",
      "owner": "Tech Lead",
      "reviewBy": "first post-RL-081 wave",
      "references": ["RL-074-F1"]
    },
    {
      "id": "AR-005",
      "title": "RL-074-F2: the auth session record's tokenDigest field name is scanner-flagged",
      "criterion": "PRD-1",
      "rowId": "SEC:findings-recorded",
      "severity": "low",
      "justification": "The RL-054 secret-shape scanner flags the FIELD NAME (key-name detector, 'token' fragment) of AuthSessionRecord.tokenDigest. The VALUE is a non-invertible SHA-256 digest — possession of the digest is not possession of the credential (verifySession requires the token; only its digest is compared) — so the substance of RL-LOCK-016 holds (proven: the live token appears in no persisted record; the digest is 64-hex). The finding records a naming-discipline inconsistency with the enterprise package's deliberate keyRef/signingKeyRef naming, which blocks wiring a repo-wide scanner sweep as a CI gate over ALL persisted records without an allow-list decision.",
      "exposure": "Scanner noise / discipline inconsistency only; no secret material is exposed.",
      "remediation": "Rename (e.g. tokenVerificationDigest) or make an explicit, documented scanner allowance for digest fields — orchestrator call. Minimal reproducer pinned in tests/security S-2.",
      "owner": "Tech Lead",
      "reviewBy": "when the scanner sweep is wired as a CI gate",
      "references": ["RL-074-F2"]
    },
    {
      "id": "AR-006",
      "title": "RL-074-F3: double session revocation surfaces a typed CAS conflict instead of an idempotent no-op",
      "criterion": "PRD-1",
      "rowId": "SEC:findings-recorded",
      "severity": "low",
      "justification": "Revoking an ALREADY-REVOKED session through a second, distinct command throws ConflictError (REVISION_CONFLICT) because the service re-saves the unchanged record whose revision no longer satisfies the stored+1 precondition. The behavior fails CLOSED (the session stays revoked; nothing reopens) — an ergonomics/idempotency gap, not a vulnerability. Fixing it means editing packages/auth — outside the release-gate worker's scope.",
      "exposure": "Operator-visible typed error on a redundant revoke; no state corruption, no reopening of revoked sessions.",
      "remediation": "Skip the save when the record is already revoked (return the recorded outcome). Minimal reproducer pinned in tests/security A-2.",
      "owner": "Tech Lead",
      "reviewBy": "first post-RL-081 wave",
      "references": ["RL-074-F3"]
    },
    {
      "id": "AR-007",
      "title": "RL-075-F1: outbox records stranded in DELIVERING have no public recovery path",
      "criterion": "PRD-2",
      "rowId": "DEPLOY:crash-recovery",
      "severity": "medium",
      "justification": "A crash between the outbox claim commit and the outcome commit leaves records in DELIVERING; claimDue only considers PENDING records and the RL-003 persistence outbox port exposes no requeue/stuck-sweep API, so a restarted worker cannot continue those obligations through the public interface (the EDGE outbox does expose recoverInFlight; the persistence outbox does not). No data is LOST — the records, payloads and digests remain intact — the invariant broken is 'restart continues in-flight work'. Fixing it means editing packages/persistence — outside the release-gate worker's scope.",
      "exposure": "Stranded in-flight outbox obligations after a crash window require manual intervention; durability of the records themselves is intact.",
      "remediation": "Add a requeueInFlight(olderThan) sweep on the port (mirroring the edge outbox's recoverInFlight), or claim with a visibility timeout so stranded claims expire back to PENDING. Minimal reproducer pinned in tests/deployment CS-4.",
      "owner": "Tech Lead",
      "reviewBy": "first post-RL-081 wave",
      "references": ["RL-075-F1"]
    },
    {
      "id": "AR-008",
      "title": "RL-073 DEFECT-1: batched inbox drains cannot progress past the first batch of a larger backlog",
      "criterion": "PRD-2",
      "rowId": "DEPLOY:crash-recovery",
      "severity": "medium",
      "justification": "AdcosWebhookInboxService.processPending(limit) re-slices the ADMITTED list from index 0 on every call; with a backlog larger than limit, the first limit records are re-considered forever and records beyond index limit are never processed. The reliability property 'every admitted record reaches a terminal state in bounded calls' fails at the inbox level; projection-level convergence still occurs via the reconciler's canonical refresh, and the deployment suites pin the workaround (an unbounded drain). Fixing it means editing packages/webhook-inbox — outside the release-gate worker's scope.",
      "exposure": "Webhook backlogs greater than the batch limit do not drain through repeated bounded processPending calls; customer-facing read models still recover through reconciliation.",
      "remediation": "Track a durable processing cursor (or mark processing state on extended records) so repeated drains advance. Minimal reproducer pinned in tests/load (webhook-ingestion-load) and re-pinned by tests/deployment CS-3.",
      "owner": "Tech Lead",
      "reviewBy": "first post-RL-081 wave",
      "references": ["RL-073-DEFECT-1"]
    },
    {
      "id": "AR-009",
      "title": "Inherent-infrastructure verification limits (the documented honest gaps of RL-074/RL-075)",
      "criterion": "PRD-1",
      "rowId": "SEC:honest-gaps",
      "severity": "medium",
      "justification": "The verification docs record the limits that cannot be closed on this tree: audit full-tail rewrite without an external anchor (I-5, limit pinned exactly), timing side channels (error-level oracle closure proven; wall-clock timing not deterministically measurable), denial-of-service volumes (admission policy verified; network-level exhaustion out of scope), secrets at rest in a real backend (port discipline verified; a vault adapter's at-rest properties are deployment work), webhook signing-key compromise operations (verifier/rotation mechanics verified; fleet-wide emergency rotation is operational work), real-database semantics (WAL/fsync nuance out of scope for in-process suites), no SQL migrations yet (runner semantics verified), single-process crash injection (CAS contract-level coverage), whole-state restore granularity (PITR is a storage-engine capability), health-check authenticity per deployment (composition honesty proven). These are the canonical 'requires real infrastructure' class the RL-081 dispatch names: recorded explicitly, never hidden.",
      "exposure": "Each limit has its strongest deterministic proxy verified and its boundary pinned by an explicit test; the residual risk materializes only when real infrastructure lands without revisiting these items.",
      "remediation": "Revisit each item as the corresponding real infrastructure lands (vault-backed secrets adapter, PostgreSQL persistence driver, anchored audit checkpoints, network-level load testing); the pinned limit tests flip when the stronger implementations arrive.",
      "owner": "Tech Lead",
      "reviewBy": "when real infrastructure replaces the in-memory adapters",
      "references": ["docs/threat-model-verification.md#3-honest-gaps", "docs/deployment-recovery.md#4-honest-gaps"]
    }
  ]
}
