{
  "schema": "roamlink/accepted-risks@1",
  "note": "Explicit, visible accepted-risk records for the RoamLink production readiness gate (RL-081). Recorded by Worker C at gate-construction time; ratification by the Tech Lead is required at merge. These records NEVER weaken a gate criterion: every criterion is still evaluated literally \u2014 an unmet row covered here is reported as a `gap` row carrying its risk ids, and any uncovered failure still fails the gate. The MVP gate (RL-080) has NO acceptance mechanism by dispatch: its failing criteria are findings for the Tech Lead, and the headline MVP finding (the \u00a711 SLO criterion, see AR-002) is what currently fails RL-080.",
  "risks": [
    {
      "id": "AR-001",
      "title": "Dogfood scenarios did not wire the observability structured logger (REMEDIATED \u2014 the world now composes the correlated logger)",
      "criterion": "PRD-3",
      "rowId": "OBS:logs-correlation",
      "severity": "medium",
      "justification": "At gate-construction time the dogfood scenarios did not wire the observability structured logger: correlation IDs were proven end-to-end through audit events only, and structured-log records were not part of the dogfood evidence. REMEDIATED on work/rl-slo-wiring-remediation (MVP-3 remediation worker, Wave 6): the dogfood world now composes createCorrelatedLogger over createInMemoryLogSink with the manual correlation carrier, and scenario 1 emits an \u00a711 SLO evaluation through logSloEvaluation under the journey's correlation-id family, asserting the log record carries the correlation id and the SLO's own redaction-safe fields. The production gate's OBS:logs-correlation row now passes on real structured-log evidence.",
      "exposure": "None remaining: structured-log records with correlation IDs are now part of the dogfood evidence (tests/dogfood/src/world.ts + scenario 1's logSloEvaluation assertion).",
      "remediation": "LANDED: tests/dogfood/src/world.ts (correlated logger + in-memory sink + manual carrier on the \u00a711 SLO plane) and scenario-1's structured-log assertion (correlation id + slo/slo_state fields). Re-run pnpm production-gate on the remediation commit: PRD-3 passes with zero gap rows.",
      "owner": "Tech Lead",
      "reviewBy": "ratified at merge (remediation landed; PRD-3 gap-free)",
      "references": [
        "GAP:dogfood-structured-logging"
      ]
    },
    {
      "id": "AR-002",
      "title": "No \u00a711 SLO was instrumented or referenced in the dogfood/load suites (REMEDIATED \u2014 the MVP-gate blocker is closed)",
      "criterion": "PRD-3",
      "rowId": "OBS:slo-wiring",
      "severity": "high",
      "justification": "spec/architecture.md \u00a711 lists nine SLOs the product must measure. The observability primitives (RL-052: makeServiceLevelObjective, SloEventRecorder, burn rates, health/metrics/log composition) can instrument every one of them \u2014 proven green by tests/release-gates/test/contract-deep.test.ts \u2014 but at gate-construction time NO dogfood or load assertion referenced any \u00a711 SLO name and no product surface wired \u00a711-named SLOs, which is why the MVP release gate (RL-080) failed its SLO criterion. REMEDIATED on work/rl-slo-wiring-remediation (MVP-3 remediation worker, Wave 6): packages/observability gained the \u00a711 SLO instrumentation surface (PRODUCT_SLO_IDS, the nine roamlink_slo_<slug> metric constants, registerProductSloMetrics, createProductSloRecorder); the reconciliation engine emits its durable-action \u00a711 measurements through an optional structural sloObserver; domain-experience exposes the pure intentSatisfactionOf mapping; and the dogfood/load suites record + assert all nine SLOs through the real recorder. Both gates now exit 0 (MVP-3: 9/9 rows pass). The record is retained for history: the gap was real, visible, and closed by the documented remediation rather than by weakening the gate.",
      "exposure": "None remaining as a verification gap: the end-to-end suites now provide mechanical SLO evidence (dogfood scenarios assert all nine \u00a711 SLOs; the load REC suite asserts SLO-E emission-volume invariants). At gate-construction time the exposure was a verification-coverage gap only \u2014 no runtime defect.",
      "remediation": "LANDED: packages/observability/src/slo/slo-metrics.ts (the \u00a711 surface), packages/reconciliation/src/slo-emission.ts + engine/boundary sloObserver wiring, packages/domain-experience/src/decision/intent-satisfaction.ts, tests/dogfood scenario/world/journey SLO wiring and assertions for all nine SLOs, tests/load SLO-E emission invariants, package tests for every new module (observability 76, reconciliation 82, domain-experience 116 green). Re-run pnpm mvp-gate and pnpm production-gate on the remediation commit: both exit 0 with MVP-3 9/9 rows passing.",
      "owner": "Tech Lead",
      "reviewBy": "ratified at merge (remediation landed; gates green)",
      "references": [
        "GAP:slo-instrumentation-dogfood-load",
        "spec/architecture.md#11-slos"
      ]
    },
    {
      "id": "AR-003",
      "title": "spec/current-state.md status block was stale post-campaign (REMEDIATED \u2014 refreshed to the delivered tree)",
      "criterion": "PRD-4",
      "rowId": "DOCS:current-state",
      "severity": "medium",
      "justification": "During the campaign spec/* was frozen by dispatch (the gates MEASURE the spec, they don't change it), so spec/current-state.md retained its pre-implementation header claims ('no production feature implementation started' / 'Repository state: greenfield') while 25 packages + 3 apps + 7 verification suites were delivered and merged. The inconsistency was recorded here instead of being silently patched by a worker. REMEDIATED post-campaign: the operator pushed the canonical status refresh (240bbc4, 'docs: refresh current implementation state') extending it to the post-gate roadmap (hosted-deployment status, RL-082..118 next phase); the Tech Lead reconciled the branch, re-ran both gates on the reconciled tree, and closed this record.",
      "exposure": "None remaining: spec/current-state.md is consistent with the implemented tree (25 packages with src/index.ts, 3 apps, 7 verification suites, both release gates exit 0) and states the hosted-deployment gap explicitly.",
      "remediation": "LANDED: operator commit 240bbc4 (canonical status refresh, extended to the post-gate roadmap) reconciled with the Tech Lead's registry update on the spec-status refresh branch; no code or gate changes. Re-run pnpm production-gate on the reconciled tree: PRD-4 DOCS:current-state passes with zero stale claims and AR-003 is no longer attached to any failing row.",
      "owner": "Tech Lead",
      "reviewBy": "ratified at merge of the spec-status refresh; regenerated artifacts committed (operator direction: next production-gate rerun)",
      "references": [
        "GAP:spec-current-state-stale"
      ]
    },
    {
      "id": "AR-004",
      "title": "RL-074-F1: secret-shaped provider references are accepted into commerce payment records",
      "criterion": "PRD-1",
      "rowId": "SEC:findings-recorded",
      "severity": "medium",
      "justification": "A malicious provider returning a JWT-shaped string as its payment 'provider reference' is accepted at admission and persisted into the commerce read model (charset permits dots/base64url). spec/data-model.md 'Privacy' says secrets/credentials are never persisted in ordinary domain tables. Exposure is bounded: the value is provider-supplied opaque REFERENCE data, not a RoamLink credential; PEM-shaped values are rejected by the charset; the repo's own RL-054 scanner flags the persisted record afterward (detection exists, admission-time enforcement does not). Fixing it means editing packages/domain-commerce \u2014 explicitly outside the release-gate worker's scope \u2014 so the finding is accepted for this release with the recorded remediation.",
      "exposure": "A secret-shaped value could be persisted in a commerce record and surfaced in read models; it is not a usable credential and is detectable by the existing scanner sweep.",
      "remediation": "Run assertNoSecretMaterial over provider-supplied reference fields at commerce admission, or further restrict the charset (e.g. no '.'). Minimal reproducer already pinned in tests/security S-6 so the fix flips an explicit assertion.",
      "owner": "Tech Lead",
      "reviewBy": "first post-RL-081 wave",
      "references": [
        "RL-074-F1"
      ]
    },
    {
      "id": "AR-005",
      "title": "RL-074-F2: the auth session record's tokenDigest field name is scanner-flagged",
      "criterion": "PRD-1",
      "rowId": "SEC:findings-recorded",
      "severity": "low",
      "justification": "The RL-054 secret-shape scanner flags the FIELD NAME (key-name detector, 'token' fragment) of AuthSessionRecord.tokenDigest. The VALUE is a non-invertible SHA-256 digest \u2014 possession of the digest is not possession of the credential (verifySession requires the token; only its digest is compared) \u2014 so the substance of RL-LOCK-016 holds (proven: the live token appears in no persisted record; the digest is 64-hex). The finding records a naming-discipline inconsistency with the enterprise package's deliberate keyRef/signingKeyRef naming, which blocks wiring a repo-wide scanner sweep as a CI gate over ALL persisted records without an allow-list decision.",
      "exposure": "Scanner noise / discipline inconsistency only; no secret material is exposed.",
      "remediation": "Rename (e.g. tokenVerificationDigest) or make an explicit, documented scanner allowance for digest fields \u2014 orchestrator call. Minimal reproducer pinned in tests/security S-2.",
      "owner": "Tech Lead",
      "reviewBy": "when the scanner sweep is wired as a CI gate",
      "references": [
        "RL-074-F2"
      ]
    },
    {
      "id": "AR-006",
      "title": "RL-074-F3: double session revocation surfaces a typed CAS conflict instead of an idempotent no-op",
      "criterion": "PRD-1",
      "rowId": "SEC:findings-recorded",
      "severity": "low",
      "justification": "Revoking an ALREADY-REVOKED session through a second, distinct command throws ConflictError (REVISION_CONFLICT) because the service re-saves the unchanged record whose revision no longer satisfies the stored+1 precondition. The behavior fails CLOSED (the session stays revoked; nothing reopens) \u2014 an ergonomics/idempotency gap, not a vulnerability. Fixing it means editing packages/auth \u2014 outside the release-gate worker's scope.",
      "exposure": "Operator-visible typed error on a redundant revoke; no state corruption, no reopening of revoked sessions.",
      "remediation": "Skip the save when the record is already revoked (return the recorded outcome). Minimal reproducer pinned in tests/security A-2.",
      "owner": "Tech Lead",
      "reviewBy": "first post-RL-081 wave",
      "references": [
        "RL-074-F3"
      ]
    },
    {
      "id": "AR-007",
      "title": "RL-075-F1: outbox records stranded in DELIVERING have no public recovery path (REMEDIATED — the persistence outbox port now exposes the recoverInFlight stuck-claim sweep)",
      "criterion": "PRD-2",
      "rowId": "DEPLOY:crash-recovery",
      "severity": "medium",
      "justification": "A crash between the outbox claim commit and the outcome commit leaves records in DELIVERING; claimDue only considers PENDING records and the RL-003 persistence outbox port exposed no requeue/stuck-sweep API, so a restarted worker could not continue those obligations through the public interface (the EDGE outbox does expose recoverInFlight; the persistence outbox did not). No data was LOST — the records, payloads and digests remained intact — the invariant broken was 'restart continues in-flight work'. REMEDIATED on work/rl-durable-recovery (Worker B, post-gate Wave 6, closing RL-093): the port gained the public stuck-claim sweep OutboxWriteRepository.recoverInFlight(at) — the persistence-side port of the edge outbox's recoverInFlight semantic — and the PostgreSQL driver mirrors it through its own locked-row claim surface. Durability laws intact: DELIVERING -> PENDING due exactly at the recovery instant WITHOUT consuming the retry budget (crash recovery is not a failed delivery attempt); payload bytes/digests untouched (idempotent replay verifies the digest; obligations are idempotency-keyed, RL-LOCK-014, so redelivery is at-least-once and safe); terminal states (DELIVERED/FAILED) are never resurrected; the sweep is transactional through the UnitOfWork and a concurrent delivered outcome wins the race as the typed ConflictError. The sweep must be called only when no live worker still holds claims (crash/restart discipline — the record carries no claim timestamp to filter by age, so the honest semantic is the full operator-visible sweep).",
      "exposure": "None remaining: the restarted worker re-owns stranded DELIVERING records through the public API and the obligations continue and complete (pinned end to end in tests/deployment CS-4, packages/persistence, and packages/persistence-postgres on real PostgreSQL).",
      "remediation": "LANDED: packages/persistence/src/outbox.ts (recoverStuckOutboxRecord pure transition + recoverInFlight on OutboxWriteRepository), packages/persistence/src/in-memory.ts (op-recorded applier + commit replay), packages/persistence-postgres/src/adapter.ts (locked-row sweep through the shared pure transition). Tests: packages/persistence/test/outbox.test.ts and packages/persistence-postgres/test/outbox.test.ts (crash-window scenario claim commits -> crash -> restart sweep -> obligation continues and completes; terminal-state, retry-budget, empty-sweep and race-conflict fences) and tests/deployment CS-4 (the former workaround pin replaced by the recovery expectation). Closing commits 2bbb0a4 (port), 297aa9c (postgres mirror), c041635 (pins). Re-run pnpm mvp-gate and pnpm production-gate on the closing commit: both exit 0.",
      "owner": "Tech Lead",
      "reviewBy": "ratified at merge of work/rl-durable-recovery (remediation landed; CS-4 verifies restart-continues-in-flight-work)",
      "references": [
        "RL-075-F1",
        "RL-093",
        "work/rl-durable-recovery"
      ]
    },
    {
      "id": "AR-008",
      "title": "RL-073 DEFECT-1: batched inbox drains cannot progress past the first batch of a larger backlog (REMEDIATED — batch progression advances past terminal records)",
      "criterion": "PRD-2",
      "rowId": "DEPLOY:crash-recovery",
      "severity": "medium",
      "justification": "AdcosWebhookInboxService.processPending(limit) re-sliced the ADMITTED list from index 0 on every call; with a backlog larger than limit, the first limit records were re-considered forever and records beyond index limit were never processed, so the reliability property 'every admitted record reaches a terminal state in bounded calls' failed at the inbox level (projection-level convergence still occurred via the reconciler's canonical refresh, and the deployment suites pinned the workaround — an unbounded drain). REMEDIATED on work/rl-durable-recovery (Worker B, post-gate Wave 6, closing RL-094): the batch is now the first `limit` records whose processing status is NOT already terminal. PROJECTED records are skipped — counted in alreadyProjected, never re-projected — WITHOUT consuming batch slots, so repeated bounded drains advance and a backlog of N records drains to completion across ceil(N/limit) calls. The admission-order guarantee, the immutability of the admitted content and the CAS conflict discipline are unchanged; FAILED records are still retried (an attempt is an attempt — it consumes one slot, but a retried head record no longer starves later records).",
      "exposure": "None remaining: repeated bounded processPending calls drain any backlog (pinned by the ceil(N/limit) drain test in packages/webhook-inbox and the flipped DEFECT-1 reproducer in tests/load; tests/deployment CS-3 now pins the bounded-drain expectation).",
      "remediation": "LANDED: packages/webhook-inbox/src/inbox.ts (terminal-state batch selection in processPending; report semantics preserved). Tests: packages/webhook-inbox/test/inbox.test.ts (backlog of 7 with limit 3 drains 3+3+1 with every record PROJECTED in admission order; terminal records do not consume slots behind a processed prefix; a retried FAILED head record does not starve later records; ordering preserved across batch boundaries), tests/load/test/webhook-ingestion-load.test.ts (the DEFECT-1 reproducer pin flipped to the fixed expectation, historical record retained) and tests/deployment CS-3 (bounded-drain pin, unbounded-drain workaround obsolete). Closing commits 2aa5e43 (fix), c041635 (pins). Re-run pnpm mvp-gate and pnpm production-gate on the closing commit: both exit 0.",
      "owner": "Tech Lead",
      "reviewBy": "ratified at merge of work/rl-durable-recovery (remediation landed; CS-3/CS-4 and the load suite verify bounded-drain completion)",
      "references": [
        "RL-073-DEFECT-1",
        "RL-094",
        "work/rl-durable-recovery"
      ]
    },
    {
      "id": "AR-009",
      "title": "Inherent-infrastructure verification limits (the documented honest gaps of RL-074/RL-075)",
      "criterion": "PRD-1",
      "rowId": "SEC:honest-gaps",
      "severity": "medium",
      "justification": "The verification docs record the limits that cannot be closed on this tree: audit full-tail rewrite without an external anchor (I-5, limit pinned exactly), timing side channels (error-level oracle closure proven; wall-clock timing not deterministically measurable), denial-of-service volumes (admission policy verified; network-level exhaustion out of scope), secrets at rest in a real backend (port discipline verified; a vault adapter's at-rest properties are deployment work), webhook signing-key compromise operations (verifier/rotation mechanics verified; fleet-wide emergency rotation is operational work), real-database semantics (WAL/fsync nuance out of scope for in-process suites), no SQL migrations yet (runner semantics verified), single-process crash injection (CAS contract-level coverage), whole-state restore granularity (PITR is a storage-engine capability), health-check authenticity per deployment (composition honesty proven). These are the canonical 'requires real infrastructure' class the RL-081 dispatch names: recorded explicitly, never hidden.",
      "exposure": "Each limit has its strongest deterministic proxy verified and its boundary pinned by an explicit test; the residual risk materializes only when real infrastructure lands without revisiting these items.",
      "remediation": "Revisit each item as the corresponding real infrastructure lands (vault-backed secrets adapter, PostgreSQL persistence driver, anchored audit checkpoints, network-level load testing); the pinned limit tests flip when the stronger implementations arrive.",
      "owner": "Tech Lead",
      "reviewBy": "when real infrastructure replaces the in-memory adapters",
      "references": [
        "docs/threat-model-verification.md#3-honest-gaps",
        "docs/deployment-recovery.md#4-honest-gaps"
      ]
    }
  ]
}