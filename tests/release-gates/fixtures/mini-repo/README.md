# Mini Fixture Repo

## Quickstart

Reproduce the local dogfood run from a clean clone:

```bash
git clone https://example.invalid/mini-repo.git
cd mini-repo
pnpm install
pnpm -C tests/dogfood test
```
