# Mini Fixture API Contract

Representative resources:

- `/v1/users`
- `/v1/orders`

Mutations distinguish `accepted` from `executed`, `executed` from `delivered`, and `delivered` from `billable-final`.
