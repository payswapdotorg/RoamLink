// the comment mentions time_to_usable_connectivity — comments are NOT assertions.
describe("violation dogfood scenario", () => {
  it("does not reference any SLO", () => {
    expect(true).toBe(true);
  });
});
